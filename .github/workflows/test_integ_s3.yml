name: Integ (s3 + gcs + minio via moto)

permissions:
  contents: read

"on":
  push:
    branches: [main]
    paths:
      - integ/**
      - data/**
      - python/**
      - .github/workflows/test_integ_s3.yml
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      pull-requests: read
    outputs:
      hit: ${{ steps.filter.outputs.hit }}
    steps:
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            hit:
              - 'integ/**'
              - 'data/**'
              - 'python/**'
              - '.github/workflows/test_integ_s3.yml'

  integ-s3:
    needs: changes
    if: ${{ !cancelled() && (github.event_name != 'pull_request' || needs.changes.outputs.hit == 'true') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install Python dependencies
        working-directory: python
        run: uv sync --all-extras --no-extra camel

      - name: Run s3 + gcs + minio integ (moto) and diff against truth
        run: |
          ./python/.venv/bin/python integ/s3.py > /tmp/s3.out
          diff integ/truth_s3.txt /tmp/s3.out

      - name: Run shared command suite on s3 (moto) and diff against shared truth
        run: |
          ./python/.venv/bin/python integ/s3_cases.py > /tmp/s3_cases.out
          diff integ/truth.txt /tmp/s3_cases.out

  gate:
    name: integ-s3-gate
    runs-on: ubuntu-latest
    needs: [changes, integ-s3]
    if: always()
    steps:
      - name: Check required jobs
        run: |
          results="${{ join(needs.*.result, ' ') }}"
          echo "$results"
          case "$results" in
            *failure*|*cancelled*) exit 1 ;;
          esac
